# React Native / Expo ont des peer-dependencies strictes qui entrent souvent en
# conflit dans un monorepo (ex. @types/react vs react-native). On autorise npm à
# résoudre malgré ces conflits — sans impact runtime (les types ne sont pas bundlés).
legacy-peer-deps=true
